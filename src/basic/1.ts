const age: number = 50;

const username: string = "Max";

const toggle: boolean = true;

const empty: null = null;

type Callback = (param1: number) => number;

const callback: Callback = (a) => {
  return 100 + a;
};
